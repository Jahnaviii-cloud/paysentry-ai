services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - "8000:8000"
  web:
    build:
      context: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api
